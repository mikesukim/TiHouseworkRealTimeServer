{
  "name": "tihouseworkreattimeserver_cloudformation",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "aws cloudformation deploy --template-file TiHouseworkRealTimeServer.yaml --stack-name TiHouseworkRealTimeServer"
  }
}
